version: '3.9'

services:
    dataStructures:
        image: 'fabiosereno/portfolio.data-structures:1.0.0'
        ports:
            - '3002:3002'
        container_name: dataStructures
        networks:
            - backend
networks:
    frontend:
    backend: