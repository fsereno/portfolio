version: '3.9'

services:
    coffeeMachine:
        image: 'fabiosereno/portfolio.coffee-machine:0.0.4'
        ports:
            - '3001:3001'
        container_name: coffeeMachine
        networks:
            - backend
networks:
    frontend:
    backend: