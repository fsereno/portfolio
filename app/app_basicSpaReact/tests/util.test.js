const chai = require("chai");

describe("Util", () => {
    describe("Test", () => {
        it("Should return true", () => {
            let result = true;
            chai.expect(result).to.equal(true);
        });
    });
});